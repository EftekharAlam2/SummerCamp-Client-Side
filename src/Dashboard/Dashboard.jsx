import { Helmet } from "react-helmet-async";

const Dashboard = () => {
  return (
    <div>
      <Helmet>
        <title>SportCamp | Dashboard</title>
      </Helmet>
    </div>
  );
};

export default Dashboard;
